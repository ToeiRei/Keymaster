version: "2"

run:
  timeout: 5m
linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    # gosimple is deprecated/merged into staticcheck; remove unsupported name
    - ineffassign
 
issues:
  exclude-use-default: false
